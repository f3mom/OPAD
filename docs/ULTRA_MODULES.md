# OPAD Ultra Modules

The Ultra pack is intentionally broad: it gives a web control surface for common A/D CTF operations while keeping destructive actions dry-run by default.

Use `/api/ultra/modules` for machine-readable module metadata and `/api/ultra/ui-map` for all web pages.
